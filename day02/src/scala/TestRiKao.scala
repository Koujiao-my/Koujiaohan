object TestRiKao {
  def main(args: Array[String]): Unit = {
    //循环输出1-30
    /*for (i <- 0 to 30) println(i)

    //偶数输出
    for(j <- 0 to 30)if (j%2==0)println(j)*/

    var o :Int =0

    while(o<50){
      o+=1
      println(o)
    }
  }
}
